#include<stdio.h>
char names(char s1[10],char s2[10]){
   // int length(int l1,int l2)
  //  l1=names(s1);
  //  l2=names(s2);
    gets(s1);
    gets(s2);
    printf("s1 is %s\n and Length is %d\n",s1,l1);
    printf("s2 is %s\n and Length is %d\n",s2,l2);
    return ;
}

int main(){
int l1,l2;
 int length(int l1,int l2)
    l1=names(s1);
    l2=names(s2);
char s1[10],s2[10];
names(s1,s2);
length(l1,l2);
return 0;}
