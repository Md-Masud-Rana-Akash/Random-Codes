#include<stdio.h>

int main(){
    int i,choice;
    int count=0;
    int low=100;
    //int count=0;
printf("choice");
scanf("%d",&choice);
int a[choice];
//int count=0;
for(i=0;i<choice;i++){
    scanf("%d",&a[i]);
    //int count=0;
    if(low>a[i])
        low=a[i];}
    printf("low is %d",low);
    for(i=0;i>choic;i--){
        if(a[i]<a[i++])
            printf("%d")
    }
    printf("")


return 0;}
